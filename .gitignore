bin/
obj/
.vs/
*.user
*.suo
*.zip

# Local machine paths — copy from environment.props.example
environment.props

# Build / local playtest copy — not source history
plugins/
.valheim-build-stamp.json
